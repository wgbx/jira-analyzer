---
description: HTML/Markdown 报告 UI 改动入口分流
globs: analyzer/report/**/*.py
alwaysApply: false
---

# 报告 UI

`analyzer/report/` 分包；公共 API 仍从 `analyzer.report` 导入。

| 报告 | 文件 / 入口 |
|------|-------------|
| 主报告（Q3/Q2 等） | `html_main.py` → `generate_html_report`；筛选 JS → `filters.py` |
| 会议块 | `meeting.py` → `_build_meeting_report_html` |
| Owner Daily | `daily.py` → `generate_owner_daily_html_report` |
| Markdown | `markdown.py` → `generate_markdown_report` |
| 共享 helpers | `common.py`（计数、颜色、条目渲染） |

- 颜色：`_owner_color` ← `OWNER_REGISTRY`；禁止第二份调色板。
- 统计口径须与 `filters.active_statuses` / README「统计口径」一致。
- 勿把分包重新合并成单文件。
- 验证：`npm start` 或 `npm run serve`；改完跑 `npm run smoke && npm test`。
