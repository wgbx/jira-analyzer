---
description: 配置、筛选口径与排期数据
globs: config.json,analyzer/config.py,analyzer/scheduled.py,data/scheduled.json,README.md
alwaysApply: false
---

# 配置与筛选

- **本地**：只维护 `config.json`（gitignore）；模板在 README「本地运行 → 配置」。
- **勿提交** Token / `config.json`。
- **Actions 默认 reports**：改 `analyzer/config.py` 的 `DEFAULT_REPORTS`（无 example 文件）。
- **多报告**：`reports[]` 的 `id` / `label` / `parent_issue` / `output`。
- **`filters.active_statuses`**：未处理统计只计这些 Jira 状态名；未配置时用代码默认 `待办` / `正在进行`。
- **排期**：改 `data/scheduled.json`（`issue` + `index`），勿硬编码进报告模块。

验证：改口径后 `npm start`；结构用 `npm run smoke`。
